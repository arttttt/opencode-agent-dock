// Constants — plugin identity and tunable limits.

export const PLUGIN_ID = "arttttt.agent-dock";
export const SERVICE = "agent-dock";

export const LIMITS = {
  labelMax: 48,
  maxRows: 50,
  pollMs: 300,
} as const;
